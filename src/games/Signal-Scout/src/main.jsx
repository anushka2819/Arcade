import React from 'react';
import ReactDOM from 'react-dom/client';
import SignalScoutGame from './SignalScoutGame';
import '../../../index.css';

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <SignalScoutGame />
  </React.StrictMode>
);
